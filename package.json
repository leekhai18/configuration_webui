{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run start --parallel",
    "bootstrap": "lerna exec npm run install:prod"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
